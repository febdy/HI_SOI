<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="comment">
	<select id="selectCommentList" parameterType="map" resultType="com.bit.hi.domain.vo.CommentVo">
		<![CDATA[
			select r.rn rn,
        		cmt_no cmtNo,
        		r.post_no postNo,
            	r.user_id userId,
                cmt_content cmtContent,
                cmt_date cmtDate
			from (select rownum rn,
                                cmt_no,
                    			post_no,
                    			user_id,
                    			cmt_content,
                    			cmt_date
        			from (select cmt_no,
                    			post_no,
                    			user_id,
                    			cmt_content,
                    			cmt_date 
                			from comments
                            where post_no=#{postNo}
                			order by cmt_date desc) o) r, post po
			where po.post_no=r.post_no
			and rn>=((#{page}-1)*5)+1
			and rn<=#{page}*5
		]]>
	</select>
	
	<insert id="apiInsertComment" parameterType="com.bit.hi.domain.vo.CommentVo">
		<selectKey keyProperty="cmtNo" resultType="int" order="BEFORE">
				select seq_cmt_no.nextval from dual
		</selectKey>
		<![CDATA[
			insert into comments
			values (#{cmtNo}, #{postNo}, #{userId}, #{cmtContent}, sysdate)
		]]>
	</insert>
	
	<select id="apiSelectComment" parameterType="int" resultType="com.bit.hi.domain.vo.CommentVo">
		<![CDATA[
			select cmt_no cmtNo,
        			post_no postNo,
            		user_id userId,
                	cmt_content cmtContent,
                	cmt_date cmtDate
            from comments
            where cmt_no=#{cmtNo}
		]]>
	</select>

</mapper>