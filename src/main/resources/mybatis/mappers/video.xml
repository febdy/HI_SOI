<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="video">

	<insert id="insertUpload" parameterType="com.bit.hi.domain.vo.VideoVo">
		<selectKey keyProperty="videoNo" resultType="int" order="BEFORE">
				select seq_video_no.nextval from dual
		</selectKey>
		<![CDATA[
			insert into video
			values (#{videoNo}, #{userId}, #{videoOriginName}, #{videoSaveName}, #{videoExName}, #{videoPath}, #{videoSize}, sysdate, 0, '교정선 받기', 0)
		]]>
	</insert>
	<!-- 모달 페이징하기 위해, mypageService.clipGetList() 로 바꿈으로써 필요없게 됨.
	<select id="selectMyVideoList" parameterType="String" resultType="com.bit.hi.domain.vo.VideoVo">
		<![CDATA[
			select video_no videoNo,
					user_id userId,
					video_origin_name videoOriginName,
					video_save_name videoSaveName,
					video_ex_name videoExName,
					video_path videoPath,
					video_size videoSize,
					to_char(video_date, 'YYYY-MM-DD') videoDate,
					video_correct_line videoCorrectLine
			from video
			where user_id=#{userId}
		]]>
	</select> -->
	
	<select id="selectVideoInfo" parameterType="int" resultType="com.bit.hi.domain.vo.VideoVo">
		<![CDATA[
			select video_no videoNo,
					user_id userId,
					video_origin_name videoOriginName,
					video_save_name videoSaveName,
					video_ex_name videoExName,
					video_path videoPath,
					video_size videoSize,
					video_date videoDate,
					video_thumnail videoThumnail,
					video_correct_line videoCorrectLine,
					video_delete videoDelete
			from video
			where video_No=#{videoNo}
		]]>
	</select>
	
	<select id="selectVideoNo" parameterType="String" resultType="int">
		<![CDATA[
			select video_no videoNo
			from video
			where video_origin_name=#{videoOriginName}
		]]>
	</select>
	
	<update id="updateThumnail" parameterType="com.bit.hi.domain.vo.VideoVo">
		<![CDATA[
			update video
			set video_thumnail=#{videoThumnail}
			where video_save_name=#{videoSaveName}
		]]>
	</update>
	
	<select id="selectCorrectedVideo" parameterType="int" resultType="com.bit.hi.domain.vo.VideoVo">
		<![CDATA[
			select video_no videoNo,
					user_id userId,
					video_origin_name videoOriginName,
					video_save_name videoSaveName,
					video_ex_name videoExName,
					video_path videoPath,
					video_size videoSize,
					video_date videoDate,
					video_thumnail videoThumnail,
					video_correct_line videoCorrectLine,
					video_delete videoDelete
			from video
			where video_No=#{videoNo}
		]]>
	</select>
	
</mapper>